; Simple test configuration
let app_name = @"Test Application"
let version = 2
let max_users = 50

{
  application => .(app_name).,
  version => .(version).,
  settings => {
    max_users => .(max_users).,
    timeout => 30,
    features => [@"auth", @"logging", @"api"]
  }
}
